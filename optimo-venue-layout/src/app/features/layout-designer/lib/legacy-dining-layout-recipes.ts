/**
 * LEGACY dining layout recipes — isolated from the normal suggestion path.
 *
 * The dining wizard must NOT import this file.
 * Kept only as a deprecated reference until the constraint generator is fully proven.
 */

export {
  DINING_LAYOUT_RECIPES,
  generateLegacyDiningLayouts,
  MAX_SUGGESTED_DINING_TEMPLATES,
} from './dining-layout-templates';
